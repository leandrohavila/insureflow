# Prisma CLI carrega automaticamente ../../.env e ../../apps/api/.env via prisma.config.ts.
# Opcional: copie variáveis aqui se rodar prisma apenas dentro de packages/database.

DATABASE_URL=postgresql://postgres:postgres@localhost:5432/insureflow?schema=public
